# ARM Cortex-A9 HDL 仿真实验学习笔记

## 📚 第一章：ARM Cortex-A9 架构深度解析

### 1.1 处理器核心特性

ARM Cortex-A9 是一款高性能、低功耗的应用处理器核心，采用 ARMv7-A 架构。

#### 核心架构特点

```
┌─────────────────────────────────────────────────────────────────┐
│                    ARM Cortex-A9 Core                           │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐  │
│  │  Fetch   │───▶│  Decode  │───▶│ Execute  │───▶│ Memory   │  │
│  │  Unit    │    │  Unit    │    │  Unit    │    │  Access  │  │
│  └──────────┘    └──────────┘    └──────────┘    └──────────┘  │
│       │               │               │               │         │
│       ▼               ▼               ▼               ▼         │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    Pipeline Registers                    │   │
│  └─────────────────────────────────────────────────────────┘   │
│       │                                                         │
│       ▼                                                         │
│  ┌──────────┐                                                   │
│  │Writeback │                                                   │
│  └──────────┘                                                   │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 流水线架构

Cortex-A9 采用可变长度流水线（8-11级），本实验简化为 **5 级流水线**：

| 阶段 | 名称 | 功能 |
|------|------|------|
| IF | Instruction Fetch | 从 I-Cache 取指令 |
| ID | Instruction Decode | 指令译码，读寄存器 |
| EX | Execute | 执行 ALU/移位操作 |
| MEM | Memory Access | 访问 D-Cache |
| WB | Write Back | 写回寄存器文件 |

### 1.3 关键流水线公式

#### 流水线吞吐量

$$
\text{Throughput} = \frac{n}{(k + n - 1) \times T_c}
$$

其中：
- $n$ = 指令数量
- $k$ = 流水线级数
- $T_c$ = 时钟周期

#### CPI (Cycles Per Instruction)

$$
\text{CPI} = \text{CPI}_{\text{ideal}} + \text{Stall}_{\text{structural}} + \text{Stall}_{\text{data}} + \text{Stall}_{\text{control}}
$$

---

## 📐 第二章：执行单元 (EXU) 设计

### 2.1 ALU 设计

#### 支持的操作

| 操作码 | 操作 | 描述 |
|--------|------|------|
| 4'b0000 | AND | 按位与 |
| 4'b0001 | EOR | 按位异或 |
| 4'b0010 | SUB | 减法 |
| 4'b0011 | RSB | 反向减法 |
| 4'b0100 | ADD | 加法 |
| 4'b0101 | ADC | 带进位加法 |
| 4'b0110 | SBC | 带借位减法 |
| 4'b0111 | RSC | 带借位反向减法 |
| 4'b1100 | ORR | 按位或 |
| 4'b1101 | MOV | 传送 |
| 4'b1110 | BIC | 位清除 |
| 4'b1111 | MVN | 取反传送 |

#### 条件标志位

```verilog
// NZCV 标志位定义
// N (Negative) : 结果为负
// Z (Zero)     : 结果为零
// C (Carry)    : 进位/借位
// V (Overflow) : 溢出
```

$$
\text{Overflow} = (A_{31} \oplus \text{Result}_{31}) \land (B_{31} \oplus \text{Result}_{31})
$$

### 2.2 Barrel Shifter 设计

ARM 指令集的强大特性之一是每条数据处理指令都可以包含移位操作。

#### 移位类型

| 类型 | 缩写 | 描述 |
|------|------|------|
| Logical Shift Left | LSL | 逻辑左移 |
| Logical Shift Right | LSR | 逻辑右移 |
| Arithmetic Shift Right | ASR | 算术右移 |
| Rotate Right | ROR | 循环右移 |
| Rotate Right with Extend | RRX | 扩展循环右移 |

#### 移位器实现原理

```
        Input[31:0]
            │
    ┌───────┴───────┐
    │  Shift Type   │
    │  & Amount     │
    └───────┬───────┘
            │
    ┌───────┴───────┐
    │   Mux Level   │ ─── 16-bit shift
    └───────┬───────┘
            │
    ┌───────┴───────┐
    │   Mux Level   │ ─── 8-bit shift
    └───────┬───────┘
            │
    ┌───────┴───────┐
    │   Mux Level   │ ─── 4-bit shift
    └───────┬───────┘
            │
    ┌───────┴───────┐
    │   Mux Level   │ ─── 2-bit shift
    └───────┬───────┘
            │
    ┌───────┴───────┐
    │   Mux Level   │ ─── 1-bit shift
    └───────┬───────┘
            │
        Output[31:0]
```

---

## 📐 第三章：控制单元 (CU) 设计

### 3.1 指令译码

ARM 指令格式（32位）：

```
31  28 27 26 25 24       21 20 19    16 15    12 11           0
┌─────┬─────┬──┬───────────┬──┬────────┬────────┬─────────────┐
│Cond │ Op  │I │  Opcode   │S │   Rn   │   Rd   │  Operand2   │
└─────┴─────┴──┴───────────┴──┴────────┴────────┴─────────────┘
```

#### 条件码

| 编码 | 助记符 | 条件 | 标志位 |
|------|--------|------|--------|
| 0000 | EQ | Equal | Z=1 |
| 0001 | NE | Not Equal | Z=0 |
| 0010 | CS/HS | Carry Set / Higher or Same | C=1 |
| 0011 | CC/LO | Carry Clear / Lower | C=0 |
| 0100 | MI | Minus (Negative) | N=1 |
| 0101 | PL | Plus (Positive or Zero) | N=0 |
| 0110 | VS | Overflow Set | V=1 |
| 0111 | VC | Overflow Clear | V=0 |
| 1000 | HI | Higher (unsigned) | C=1 and Z=0 |
| 1001 | LS | Lower or Same (unsigned) | C=0 or Z=1 |
| 1010 | GE | Greater or Equal (signed) | N=V |
| 1011 | LT | Less Than (signed) | N≠V |
| 1100 | GT | Greater Than (signed) | Z=0 and N=V |
| 1101 | LE | Less or Equal (signed) | Z=1 or N≠V |
| 1110 | AL | Always | - |

### 3.2 流水线冒险处理

#### 数据冒险类型

1. **RAW (Read After Write)** - 最常见
   ```
   ADD R1, R2, R3    ; 写 R1
   SUB R4, R1, R5    ; 读 R1 (需要前递)
   ```

2. **WAR (Write After Read)** - 顺序流水线中不存在

3. **WAW (Write After Write)** - 顺序流水线中不存在

#### 前递路径

```
EX/MEM.Rd ──────────────┐
                        ▼
                   ┌─────────┐
MEM/WB.Rd ─────────│   MUX   │──▶ ALU 输入
                   └─────────┘
                        ▲
ID/EX.Rs ───────────────┘
```

---

## 📐 第四章：缓存系统设计

### 4.1 Cache 基本参数

本实验采用简化的 Direct-Mapped Cache：

| 参数 | 值 |
|------|-----|
| Cache 大小 | 4KB |
| 块大小 | 16 bytes |
| 块数 | 256 |
| 关联度 | 1 (Direct-Mapped) |

#### 地址分解

```
31                    12 11        4 3      0
┌────────────────────────┬──────────┬────────┐
│         Tag            │  Index   │ Offset │
│       (20 bits)        │ (8 bits) │(4 bits)│
└────────────────────────┴──────────┴────────┘
```

### 4.2 Cache 性能公式

#### 平均访存时间 (AMAT)

$$
\text{AMAT} = \text{Hit Time} + \text{Miss Rate} \times \text{Miss Penalty}
$$

#### Cache Miss 分类 (3C Model)

1. **Compulsory Miss** - 首次访问
2. **Capacity Miss** - 容量不足
3. **Conflict Miss** - 映射冲突

---

## 📐 第五章：存储接口设计

### 5.1 AXI-Lite 接口简介

本实验采用简化的 AXI-Lite 协议：

#### 写通道

| 信号 | 方向 | 描述 |
|------|------|------|
| AWADDR | Master→Slave | 写地址 |
| AWVALID | Master→Slave | 写地址有效 |
| AWREADY | Slave→Master | 写地址就绪 |
| WDATA | Master→Slave | 写数据 |
| WVALID | Master→Slave | 写数据有效 |
| WREADY | Slave→Master | 写数据就绪 |
| BRESP | Slave→Master | 写响应 |
| BVALID | Slave→Master | 写响应有效 |
| BREADY | Master→Slave | 写响应就绪 |

#### 读通道

| 信号 | 方向 | 描述 |
|------|------|------|
| ARADDR | Master→Slave | 读地址 |
| ARVALID | Master→Slave | 读地址有效 |
| ARREADY | Slave→Master | 读地址就绪 |
| RDATA | Slave→Master | 读数据 |
| RRESP | Slave→Master | 读响应 |
| RVALID | Slave→Master | 读数据有效 |
| RREADY | Master→Slave | 读数据就绪 |

### 5.2 握手协议

AXI 采用 Valid-Ready 握手机制：

```
        ┌─────────────────┐
        │                 │
VALID ──┤                 ├── 数据传输
        │                 │   (VALID & READY)
READY ──┤                 │
        │                 │
        └─────────────────┘
```

传输发生条件：`VALID && READY` 在同一时钟上升沿

---

## 📐 第六章：同步单元设计

### 6.1 时钟域同步

#### 两级同步器

```verilog
// 异步信号同步到目标时钟域
always @(posedge clk or negedge rst_n) begin
    if (!rst_n) begin
        sync_reg1 <= 1'b0;
        sync_reg2 <= 1'b0;
    end else begin
        sync_reg1 <= async_in;
        sync_reg2 <= sync_reg1;
    end
end

assign sync_out = sync_reg2;
```

#### MTBF 计算

$$
\text{MTBF} = \frac{e^{t_r/\tau}}{f_c \cdot f_d \cdot T_0}
$$

其中：
- $t_r$ = 同步器分辨时间
- $\tau$ = 亚稳态时间常数
- $f_c$ = 时钟频率
- $f_d$ = 数据变化频率

### 6.2 异步 FIFO 设计要点

1. 使用格雷码进行指针同步
2. 读写指针需要跨时钟域同步
3. 空/满标志需要保守判断

---

## 💡 关键设计技巧

### Tip 1: 流水线寄存器命名规范

```verilog
// 使用阶段前缀命名
wire [31:0] id_instruction;  // ID 阶段指令
reg  [31:0] ex_instruction;  // EX 阶段指令 (流水线寄存器)
wire [31:0] mem_alu_result;  // MEM 阶段 ALU 结果
```

### Tip 2: 参数化设计

```verilog
module cache #(
    parameter CACHE_SIZE = 4096,      // Cache 总大小 (bytes)
    parameter BLOCK_SIZE = 16,        // 块大小 (bytes)
    parameter ADDR_WIDTH = 32         // 地址宽度
) (
    // 端口定义
);

localparam NUM_BLOCKS = CACHE_SIZE / BLOCK_SIZE;
localparam INDEX_WIDTH = $clog2(NUM_BLOCKS);
localparam OFFSET_WIDTH = $clog2(BLOCK_SIZE);
localparam TAG_WIDTH = ADDR_WIDTH - INDEX_WIDTH - OFFSET_WIDTH;
```

### Tip 3: 使用 generate 语句简化重复逻辑

```verilog
genvar i;
generate
    for (i = 0; i < 32; i = i + 1) begin : reg_gen
        always @(posedge clk) begin
            if (we && (wd_addr == i) && (i != 0))
                registers[i] <= wd;
        end
    end
endgenerate
```

---

## 📊 调试技巧

### Vivado 波形调试

1. 添加关键信号到波形窗口
2. 设置合适的时间刻度
3. 使用标记点跟踪关键事件
4. 利用触发条件捕获特定状态

### 常见错误排查

| 症状 | 可能原因 | 解决方法 |
|------|----------|----------|
| 全 X 状态 | 未初始化 | 检查复位逻辑 |
| 时序违例 | 组合逻辑过长 | 插入流水线寄存器 |
| 功能错误 | 前递缺失 | 检查冒险单元 |

---

*本笔记基于 ARM Cortex-A9 Technical Reference Manual 和顶级 EE 院校教材编写。*
